- type: gameMap
  id: Split
  mapName: 'Split'
  mapPath: /Maps/split.yml
  minPlayers: 0
  stations:
    Split:
      mapNameTemplate: '{0} Split {1}'
      nameGenerator:
        !type:NanotrasenNameGenerator
        prefixCreator: 'SW'
      overflowJobs:
      - Passenger
      availableJobs:
        Passenger: [ -1, -1 ]
